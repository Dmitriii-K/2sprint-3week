import { RegistrationEmailResending } from "../input-output-types/auth-type";

export const authUser = async (
    req: Request<{}, {}, RegistrationEmailResending>,
    res: Response
) => {
    try {
        
    } catch (error) {
        console.log(error);
    }
}

204
400